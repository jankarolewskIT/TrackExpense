# from django.db.models import Model, OneToOneField, DO_NOTHING, DateTimeField, CASCADE
#
# from .budget import Expence
#
#
# class ExpenseArchive(Model):
#     expense = OneToOneField(Expence, on_delete=CASCADE)
#     date = DateTimeField(auto_now_add=True)
